# task-enrichment-rules.md

Pattern-matched onto each task before execute-task dispatches the implementer. Use for domain-specific enrichment — e.g., "when task mentions 'auth', inject reminder about session token handling."

Leave empty for plugin defaults.
