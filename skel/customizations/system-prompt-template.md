# system-prompt-template.md

Content here is prepended to every subagent spawn by the plugin. Use for persistent context the plugin should pass to every worker — codebase conventions, domain terminology, stylistic guardrails.

Leave empty for plugin defaults.
